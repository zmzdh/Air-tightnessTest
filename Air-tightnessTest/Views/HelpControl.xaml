<UserControl x:Class="LumbarMassageTest.UserControls.HelpControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <SolidColorBrush x:Key="BluePrimary" Color="#2563EB"/>
        <SolidColorBrush x:Key="BlueDark" Color="#1E40AF"/>
        <SolidColorBrush x:Key="BlueLight" Color="#EAF2FF"/>
        <SolidColorBrush x:Key="BlueBorder" Color="#C7D2FE"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1E293B"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource BlueLight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BluePrimary}" Padding="20" CornerRadius="6" Margin="20,20,20,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="使用帮助" Foreground="White" FontSize="26" FontWeight="Bold"/>
                <Button x:Name="BtnReload"
                        Content="重新加载"
                        Margin="20,0,0,0"
                        Padding="18,6"
                        Foreground="White"
                        Background="{StaticResource BlueDark}"
                        BorderBrush="{StaticResource BlueDark}"
                        Click="BtnReload_OnClick"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" Margin="20,10,20,20" Padding="20" CornerRadius="6"
                BorderBrush="{StaticResource BlueBorder}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="TxtHelpContent"
                           TextWrapping="Wrap"
                           FontSize="16"
                           Foreground="{StaticResource TextPrimary}"
                           xml:space="preserve"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
